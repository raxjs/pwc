{
  "workspaces": [
    "packages/*"
  ],
  "name": "rax-next-project",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "setup": "npm run clean && pnpm install --reporter=append-only",
    "clean": "rm -rf node_modules packages/*/lib packages/*/es packages/*/dist"
  },
  "dependencies": {
    "@commitlint/cli": "^11.0.0",
    "@appworks/spec": "^1.3.2",
    "@types/eslint": "^7.28.0",
    "eslint": "^8.0.0",
    "ts-node": "^8.6.1",
    "typescript": "^4.0.0",
    "stylelint": "^14.2.0"
  }
}
